# homebrew-tap (deprecated)

This tap is no longer maintained. The MeowPass formula has moved.

```sh
brew untap chpecson/tap
brew tap meowrithm/tap
brew install meowpass
```

The last formula published here was `meowpass` v0.1.4. Current releases live at
[meowrithm/homebrew-tap](https://github.com/meowrithm/homebrew-tap).
